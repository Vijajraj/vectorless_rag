# Ollama runs locally — no API key needed.
# Default base URL is http://localhost:11434/v1
# Override only if your Ollama instance is on a different host/port.
# OLLAMA_BASE_URL=http://localhost:11434/v1
